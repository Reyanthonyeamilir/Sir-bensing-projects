

<form method="post" action="{{ path('app_petproducts_delete', {'id': petproduct.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ petproduct.id) }}">
    <button class="btn">Delete</button>
</form>
